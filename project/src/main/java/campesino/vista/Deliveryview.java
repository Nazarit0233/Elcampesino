package campesino.vista;

import javax.swing.JFrame;

public class Deliveryview extends JFrame {
    public Deliveryview() {
        setTitle("Vista de Repartidor");
        setSize(800, 600);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
    
}
